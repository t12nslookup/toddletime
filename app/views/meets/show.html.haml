%h1 Meet Details
%table.table.table-condensed.table-striped.table-hover
  %tr
    %th Date of meet
    %td= @meet.meet_date.try :to_s, :long_ordinal
  - if @meet.meet_type.present?
    %tr
      %th Meet Type
      %td= @meet.meet_type.name

%h1 Leader Details
%table.table.table-condensed.table-striped.table-hover
  %thead
    %tr
      %th Name
      %th Job
  %tbody
    - @meet.leader_to_meets.includes(:leaders).includes(:jobs).each do |leader2meet|
      %tr
        %td= leader2meet.leaders.name
        %td= leader2meet.jobs.name
    %tr
      %td{:align => 'center', :colspan => 3}= link_to 'Set Leaders', edit_meet_path(@meet)
%ul.nav.nav-pills
  %li= link_to 'View All Meets', meets_path
