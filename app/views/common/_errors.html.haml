- if object.errors.any?
  #error_explanation
    %h2
      = pluralize(object.errors.count, "error")
      prevented the creation of this #{object.class.to_s.humanize}:
    - object.errors.full_messages.each do |msg|
      %li= msg
